name: Lint, test and deploy in three jobs
on: push
jobs:
    lint:
        runs-on: ubuntu-latest
        steps:
          - name: Run lint
            run: echo "npm lint"
    test:
        needs: lint
        runs-on: ubuntu-latest
        steps:
          - name: Run test
            run: echo "npm test"
    deploy:
        needs: test
        runs-on: ubuntu-latest
        steps:
          - name: Run build
            run: echo "npm build"